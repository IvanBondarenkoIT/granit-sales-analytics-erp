# Django settings
DEBUG=True
SECRET_KEY=your-secret-key-here-change-in-production

# Database settings (local development)
DB_NAME=granitanalytics
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=db
DB_PORT=5432

# Production deployment notes:
# - Railway: Set DATABASE_URL in Railway variables (uses internal Postgres or external alt)
# - alt Debian ETL: Configure connection to Postgres on same host or remote
# - firebird-db-proxy: ETL scripts will use proxy API URL (not stored here)
